# character-editor — Codebase index
> Generated by /vibe:sync on 2026-08-16. Re-run to update.

## Modules
- [`modules/app.md`](modules/app.md) — application entry point, mounts the app into the DOM as a `web-ui-kit` app shell

## Observed patterns
- TypeScript with `strict`, `noUnusedLocals`, `noUnusedParameters` enabled (`tsconfig.json`)
- Explicit `.ts` extensions on relative imports (`allowImportingTsExtensions`)
- Test files co-located with source as `*.test.ts`, using Vitest + jsdom
- Ambient module declarations for untyped external packages live in `src/types/`
- Non-critical error states use tokens; the "design system unavailable" error state deliberately avoids design-system tokens, so it renders even if the design system failed to load

## Other context files
- [`models.md`](models.md) — data models
- [`glossary.md`](glossary.md) — ubiquitous language
- [`decisions/`](decisions/) — architectural decision records
