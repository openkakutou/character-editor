# Data models

No data models yet — the codebase is at scaffold stage (a single exported version constant). Character/sprite/palette/animation/state/command types will be documented here as the WASM bridges and editors land.
